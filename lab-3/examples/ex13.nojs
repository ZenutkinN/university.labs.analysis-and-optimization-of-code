int fact(int num) {
  if (num == 0 || num == 1)  {
  	return 1
  } else { 

    if (num > 1) {
  	    return num * fact(num - 1)
    } else {
        return -1
    }

  }

  return 0
}

int main() {
	stdout(fact(6))
  stdout("\n")
  
	return 0
}